import { Header, LoginForm } from '../../components/index';

const Login = () => {
  return (
    <div className="flex flex-col items-center justify-center h-full flex-nowrap">
      <Header />
      <LoginForm />
    </div>
  );
};

export default Login;
