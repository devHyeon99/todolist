import { Header, RegisterForm } from '../../components/index';

const Register = () => {
  return (
    <div className="flex flex-col items-center justify-center h-full flex-nowrap">
      <Header />
      <RegisterForm />
    </div>
  );
};

export default Register;
